{
  "name": "@openclaw/beagle-channel",
  "version": "0.1.0",
  "description": "OpenClaw Beagle/Elastos Carrier channel plugin",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && node-gyp rebuild",
    "prebuild": "node-gyp configure",
    "clean": "rm -rf dist build",
    "test": "echo 'Tests to be added'"
  },
  "keywords": [
    "openclaw",
    "beagle",
    "elastos",
    "carrier",
    "channel",
    "plugin"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.0.0",
    "node-gyp": "^10.0.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "node-addon-api": "^8.0.0"
  },
  "openclaw": {
    "extensions": [
      "./extension/index.ts"
    ]
  }
}
